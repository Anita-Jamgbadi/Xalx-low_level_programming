#include "main.h"
#include <string.h>

/**
 * _strlen - returns the lenth of a string
 * @s: string to be scrutinized
 * Return: 0
 */

int _strlen(char *s)
{
	char ch[] = "*s";

	return (strlen(ch));
}
