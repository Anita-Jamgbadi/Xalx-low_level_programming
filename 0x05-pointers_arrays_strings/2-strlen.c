#include "main.h"
#include <string.h>

/**
 * _strlen - returns the lenth of a string
 * @s: string to be scrutinized
 * Return: 0
 */

int _strlen(char *s)
{
	char f;
	
	f = *s;
	char ch[] = "f";

	return (strlen(ch));
}
