#include "main.h"
#include <stdlib.h>

/**
 * _puts - prints strint to stdoutput
 * @str:  string to be printed
 * Return: 0;
 */

void _puts(char *str)
{
	char *new;

	new = (char *)malloc(150 * sizeof(char));

	_putchar(new);
}
