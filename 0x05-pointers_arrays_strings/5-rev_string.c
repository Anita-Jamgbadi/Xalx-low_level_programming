#include "main.h"

/**
 * rev_string -reverses string
 * @s: holds string to be reversed
 * Return: 0
 */

void rev_string(char *s)
{
	int i, k;

	for (i = 0; s[i] != '\0'; i++)
	{
		k = i;
	}

	while (k > 0)
	{
		_putchar(s[k]);
	}
}
