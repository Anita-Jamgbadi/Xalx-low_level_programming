#include "main.h"

/**
 * print_array - print n of the length of a
 * @a: array pointer
 * @n: number of values to print
 */

void print_array(int *a, int n)
{
	int k, g, b;

	for (k = 0; s[k] != '\0'; k++)
	{
		g = k;
	}

	for (g; g != n; g--)
	{
		_putchar("%d, ", a[g]);
	}
	_putchar('\n');
}
